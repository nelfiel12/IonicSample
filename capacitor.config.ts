import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pscheol.ionicsample',
  appName: 'IonicSampleApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
